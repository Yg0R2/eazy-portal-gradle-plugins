package org.eazyportal.gradle.eazyproject

/**
 * Default versions used as conventions by [EazyProjectExtension] properties (design §5.1).
 * Grows as more defaults are added — see the README for the full table.
 */
object DefaultVersions {

    const val EXAMPLE_CORE_DEFAULT_VERSION = "1.2.3"

}
