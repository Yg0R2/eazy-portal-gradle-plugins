import org.eazyportal.plugin.release.core.scm.model.ConventionalCommitType
import org.eazyportal.plugin.release.core.version.model.VersionIncrement

plugins {
    java
    id("org.eazyportal.plugin.gradle.release-gradle")
}

eazyRelease {
    conventionalCommitTypes = listOf(
        ConventionalCommitType(listOf("dummy"), VersionIncrement.MAJOR)
    )
}
